function addCounterSlice(state) {
  return { zusCounter: state.counter + 1 };
}
function decreaseCounterSlice(state) {
  return { zusCounter: state.counter + 1 };
}
export { addCounterSlice, decreaseCounterSlice };
